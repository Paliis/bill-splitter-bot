# Роудмап і порядок робіт

Документ описує **етапи**, **артефакти**, які оновлювати після кожного кроку, і **відкриті питання**, що потребують вашого рішення **або додаткових даних**. Терміни й оцінки в **людино-днях не проставлені** — їх можна додати, коли будуть вхідні дані.

**Узгоджені архітектурні рішення** (валюта на поїздку, locale, party, settlement тощо): [ARCHITECTURE.md](./ARCHITECTURE.md) §0.

---

## Як рухаємось (домовленість)

1. **Покроково:** завершуємо крок → оновлюємо цей файл (статус етапу, закриті питання) + при змінах продукту/коду — [CHANGELOG.md](./CHANGELOG.md); при зміні правил продукту для людей — [PRODUCT.md](./PRODUCT.md).
2. **Невизначеність:** якщо для кроку **не вистачає даних** або є **продуктовий вибір** — **питаємо вас**, не заповнюємо припущеннями.
3. **Поточний фокус:** див. розділ **«Активний етап»** нижче — він змінюється лише явним рішенням після закриття попереднього кроку або зміни пріоритету.

---

## Артефакти (що і коли чіпати)

| Артефакт | Коли оновлювати |
|----------|------------------|
| **ROADMAP.md** (цей файл) | Початок/кінець кроку; нове відкрите питання; зміна активного етапу; закриття питання (позначити **Вирішено** + коротка формулювання рішення або посилання на § у ARCHITECTURE/PRODUCT). |
| **ARCHITECTURE.md** | Нова домовленість по системі (API, дані, безпека); зміни в §0 за потреби через вас. |
| **PRODUCT.md** | Зміна сценаріїв або правил для користувача, нові команди/обмеження. |
| **CHANGELOG.md** | Будь-яка змістовна зміна в репозиторії (код, доки, деплой-конфіг). |
| **legal/** | Чернетки Privacy / Terms; після правок юриста — оновити дати, плейсхолдери, URL. |
| **openapi/** | Контракт **openapi.yaml**; при зміні полів БД або URL — оновити й узгодити з клієнтами. |
| **STEP_BY_STEP.md** | Повна покрокова інструкція для власника (BotFather, Neon/Postgres, Render, env, перевірки). |
| **DEPLOY_RENDER.md** | Скорочений опис двох сервісів; посилання на **STEP_BY_STEP**. |
| **scripts/verify_local.py** | Швидка перевірка без сервера; оновлювати, якщо змінюється структура пакетів. |
| **.github/workflows/ci.yml** | CI на `main` / PR; додавати кроки обережно (секрети не логувати). |

---

## Активний етап

| Поле | Значення |
|------|----------|
| **Фаза** | **1 — Ядро API** |
| **Крок** | **1.2** FastAPI у **[backend/](../backend/)**: `GET /health`, `GET /v1/trips/{tripId}`; Render **Web** `bill-splitter-api` у [render.yaml](../render.yaml). |
| **Наступна дія** | **1.3** Розширити API (members, expenses, balances) + **JWT** або Telegram bridge замість `X-Api-Secret` для публічного PWA. |

*Після переходу на Фазу 1 оновіть таблицю вище (фаза, крок, наступна дія).*

---

## Фази (огляд)

### Фаза 0 — Підготовка і вирівнювання

- [x] ARCHITECTURE §0 (узгоджені рішення).
- [x] ROADMAP + перелік відкритих питань.
- [x] Рішення по **RD-001 … RD-006** (зафіксовано в таблиці нижче + PRODUCT).
- [x] Скелет **OpenAPI** [openapi/openapi.yaml](./openapi/openapi.yaml) + [README](./openapi/README.md).

### Фаза 1 — Ядро: HTTP API + спільний домен з ботом

- [x] Каркас FastAPI: `GET /health`, `GET /v1/trips/{tripId}` ([backend/main.py](../backend/main.py)); опційний **`X-Api-Secret`** якщо задано **`API_SECRET`**.
- [ ] Решта **REST API v1** за OpenAPI (members, expenses, balances, settlement, …).
- [ ] **Settlement** — той самий алгоритм, що й у боті (мінімізація переказів); покрити тестами.
- [ ] **Бот** або викликає API, або спільний сервісний шар без дублювання правил (узгодити на кроці імплементації після RD).
- [ ] **Авторизація API**: заготовка під JWT та мапінг Telegram → `user_id` (мінімум для внутрішніх викликів).

### Фаза 2 — PWA MVP

- [ ] Проєкт PWA (окрема папка/репо — рішення на Фазі 1).
- [ ] Логін (пріоритет: **Telegram** для існуючих користувачів за [ARCHITECTURE.md](./ARCHITECTURE.md) §0.6).
- [ ] Екрани: список поїздок / деталь, додати витрату, статус, settlement (відображення результату API).
- [ ] **Manifest + Service Worker** (базово).
- [ ] **Мова:** профіль + сесійне перевизначення (як у §0.5).
- [ ] **Валюта:** вибір при створенні поїздки (§0.4).

### Фаза 3 — Інтеграція з Telegram як каналом до PWA

- [ ] **WebApp / Mini App** (кнопка відкриття PWA з чату) — за наявності домену та налаштувань BotFather.
- [ ] Deep links на конкретну витрату/поїздку — після стабільних `id` у API.

### Фаза 4 — PWA + магазини (стратегічний пріоритет **RD-005**)

- Паралельно з ботом як каналом входу: **PWA** як основний детальний інтерфейс + **iOS/Android** (платний або безкоштовний — залежно від моделі в сторах).
- [ ] OAuth Google / Apple, пуші (заготовка під токени вже в архітектурі).
- [ ] Публікація в **App Store / Google Play**; оновлення **[legal/](./legal/)** під правила магазинів після рев’ю юриста.

*Telegram WebApp / deep links (Фаза 3) залишаються мостом до PWA, а не єдиним «дімом» продукту — узгоджено **RD-005**.*

---

## Чеклист власника

Виконуйте по черзі; що залежить лише від коду в репо, можна закрити без хостингу.

### Render (прод)

- [ ] У **PostgreSQL** (Render / Neon / інше) є жива БД; URI вигляду **`postgresql+asyncpg://...`** у обох сервісах.
- [ ] **Worker** `bill-splitter-bot`: задані **`BOT_TOKEN`**, **`DATABASE_URL`**; деплой «зелений», у логах немає циклічних `TelegramConflictError` (один процес на токен).
- [ ] **Web** `bill-splitter-api`: задані **`DATABASE_URL`** (той самий) і **`API_SECRET`**; **`BOT_TOKEN` не додавати**.
- [ ] **Start command** Web точно: `PYTHONPATH=. uvicorn backend.main:app --host 0.0.0.0 --port $PORT` (як у [render.yaml](../render.yaml)).
- [ ] Відкрити **публічний URL** API: `GET /health` → `{"status":"ok"}`.
- [ ] Перевірити `GET /v1/trips/<існуючий_id>` із заголовком **`X-Api-Secret`** → JSON поїздки або очікуваний **404**.

Детальніше: покроково [STEP_BY_STEP.md](./STEP_BY_STEP.md); скорочено [DEPLOY_RENDER.md](./DEPLOY_RENDER.md).

### Локально (розробка)

- [ ] Python **3.11** (див. [runtime.txt](../runtime.txt)); `pip install -r requirements.txt`.
- [ ] З кореня репо: `python scripts/verify_local.py` (або вручну `PYTHONPATH=.` + імпорт з [backend/README.md](../backend/README.md)).
- [ ] Запуск API: `PYTHONPATH=. uvicorn backend.main:app --reload --host 127.0.0.1 --port 8000`; перевірити `/health` і за потреби `/v1/trips/...`.

### Автоперевірка в репозиторії

- Після push на **`main`**: workflow **[`.github/workflows/ci.yml`](../.github/workflows/ci.yml)** — `compileall`, імпорт `backend.main` (GitHub Actions; не потребує ваших секретів).

### Юридичне (коли виходите в публічний PWA / стори)

- [ ] Заповнити плейсхолдери в **[legal/](./legal/)** і пройти **рев’ю юриста** (див. [legal/README.md](./legal/README.md)).

---

## Рішення (колишні RD)

| ID | Питання | Статус | Рішення / коментар |
|----|---------|--------|---------------------|
| **RD-001** | Хто може **редагувати / видаляти** витрату? Історія змін у MVP? | **Вирішено** | **Лише автор** витрати. **Аудит історії в MVP — ні.** |
| **RD-002** | Після **settled**? | **Вирішено** | **Архів** (без зміни фактів після settle в продуктовій цілі); **перегляд** минулих подій обов’язково. |
| **RD-003** | Монетизація | **Вирішено** | **Зараз — донати**; **надалі можливий freemium** + **можлива оплата за додаток у сторах**. |
| **RD-004** | Фото чеків у MVP? | **Вирішено** | **Ні.** |
| **RD-005** | «Дім» продукту 12–18 міс. | **Вирішено** | **PWA + стори**; бот — канал входу та швидких дій / Mini App. |
| **RD-006** | Privacy / Terms | **Вирішено** | Чернетки **[legal/](./legal/)** + чеклист для юриста; перед продом — **перевірка юристом**. |

*Нові питання — **RD-007** … (поки немає).*

---

## Журнал змін цього роудмапу

| Дата | Зміна |
|------|--------|
| 2026-03-26 | Перша версія: фази 0–4, артефакти, RD-001–006, активний етап = Фаза 0. |
| 2026-03-27 | Закрито RD-001…006; додано **legal/**; **Фаза 4** уточнена (PWA + стори); активний крок → OpenAPI / v1. |
| 2026-03-27 | Додано **docs/openapi/**; Фаза 0 закрита по контракту; **Фаза 1 крок 1.2** — імплементація HTTP. |
| 2026-03-27 | Схема **A** на Render: worker + **web** `bill-splitter-api`; **backend/main.py**, `API_SECRET`, оновлено **render.yaml**. |
| 2026-03-27 | **Чеклист власника** у ROADMAP; **DEPLOY_RENDER.md**; **scripts/verify_local.py**; **GitHub Actions CI**. |
| 2026-03-27 | **[STEP_BY_STEP.md](./STEP_BY_STEP.md)** — детальна покрокова інструкція деплою та перевірок. |

*При оновленні додавайте рядок з датою і одним реченням.*
